PORT=8080
APP_ENV=development  # set to "production" on Fly.io to enable Secure cookies
# Use Neon's pooler URL (hostname contains -pooler) for the server.
# Use the direct URL (no -pooler) for local dev.
DATABASE_URL=postgres://localhost:5432/gather?sslmode=disable
GATHER_HMAC_SECRET=replace-with-output-of-make-secrets-hmac
